import './TitleBar.scss'

function TitleBar() {
  return (
    <div className="title-bar">
      <img src="./logo.png" alt="密语" className="title-logo" />
      <span className="titles">密语</span>
    </div>
  )
}

export default TitleBar
